info:
  name: Add Group 1
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}/api/v1/add_group"
  body:
    type: json
    data: |-
      {
        "group_id":"group_1",
        "member_list":["pt1@example.com","pt2@example.com"],
        "consent_start_date":"2026-01-01",
        "consent_end_date":"2026-04-23",
        "warmup":true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
