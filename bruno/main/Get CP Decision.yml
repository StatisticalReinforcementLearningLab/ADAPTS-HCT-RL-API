info:
  name: Get CP Decision
  type: http
  seq: 4

http:
  method: POST
  url: "http://127.0.0.1:5055/api/v1/action"
  body:
    type: json
    data: |-
      {
        "decision_type": "cp_message",
        "group_id": "{{group_id}}",
        "decision_idx": 1,
        "timestamp": "2026-01-01T08:00:00.000Z"
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "201"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
