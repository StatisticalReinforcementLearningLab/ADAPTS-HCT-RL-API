info:
  name: Add Group 2
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseURL}}/api/v1/add_group"
  body:
    type: json
    data: |-
      {
        "group_id":"group_2",
        "member_list":["pt3@example.com","pt4@example.com"],
        "consent_start_date":"2026-01-01",
        "consent_end_date":"2026-04-23",
        "warmup":true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
