info:
  name: Run Update
  type: http
  seq: 6

http:
  method: POST
  url: "http://127.0.0.1:5055/api/v1/update"
  body:
    type: json
    data: |-
      {
        "timestamp": "2026-01-01T08:00:00.000Z"
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "202"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
